# ROADMAP (v0.5 → v1)

## v0.5 (this workspace) — Trade model layer
- SL: structural invalidation (zone-based) + buffer
- TP: liquidity pools (EQH/EQL, PDH/PDL, ext range) with RR fallback
- Simple trade simulator (partials + BE)

## v0.6 — Broker economics + determinism
- Spread/slippage/commission models + tick-value conversions
- Golden vectors + hash fixtures (no lookahead)
- Parameter sweeps + report packs (Plotly.NET optional)

## v0.7 — Strategy research enablement
- Multi-position rules, pyramiding options
- Regime filters and session filters
- Evidence pack exporter (all artifacts bundled + metadata)

## v1.0
- Production-ready strategy core + broker-grade economics + UI presets
